#include "main.h"
/**
 * _isupper - check the code
 * @c: prints if its uppercase
 * Return: 1 if its upper and 0 otherwise
 */
int _isupper(int c)
{
	if (c == 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
